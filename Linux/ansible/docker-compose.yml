version: '2'

services:
    ansibleslave:
        image: liveforensics/ansibleslave:2.7.6
        container_name: ansibleslave
        hostname: ansibleslave
        build:
            context: .
            dockerfile: Dockerfile
        networks:
            - local-network
                    
networks:
    local-network:
        driver: bridge
        
